import { APIAsset } from "types"

type Props = {
  title: string
  text: string
  subtext: string
  image: APIAsset
}

const Home = ({ title, text, subtext, image }: Props): React.ReactElement => {
  return <div> </div>
}

export default Home
