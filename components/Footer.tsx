type Props = {
  title: string
  text: string
  copyright: string
  contact: string
  email: string
}

const Footer = ({
  title,
  text,
  copyright,
  contact,
  email,
}: Props): React.ReactElement => {
  return <div> </div>
}

export default Footer
